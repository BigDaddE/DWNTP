{
	"name": "dwntp-caliper-benchmark",
	"version": "1.0.0",
	"description": "Hyperledger Caliper benchmark for DWNTP",
	"scripts": {
		"bind": "npx caliper bind --caliper-bind-sut fabric:2.2",
		"benchmark": "npm run bind && npx caliper launch manager --caliper-workspace ./ --caliper-networkconfig network-config.yaml --caliper-benchconfig benchmark-config.yaml --caliper-flow-only-test"
	},
	"dependencies": {
		"@hyperledger/caliper-cli": "^0.5.0",
		"@hyperledger/caliper-fabric": "^0.5.0",
		"fabric-network": "^2.2.12"
	},
	"author": "DWNTP",
	"license": "GPL-3.0"
}
